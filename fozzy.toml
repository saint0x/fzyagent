[package]
name = "fzyagent"
version = "0.1.0"

[[target.bin]]
name = "fzyagent"
path = "src/main.fzy"

[unsafe]
contracts = "compiler"
enforce_dev = false
enforce_verify = true
enforce_release = true
