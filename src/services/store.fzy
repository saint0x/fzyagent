use core.fs;

fn init() -> i32 {
    runtime.config.ensure_state_root()
    fs.mkdir(apputil.path_join2(runtime.config.state_root(), "runs"))
    fs.mkdir(apputil.path_join2(runtime.config.state_root(), "tools"))
    return 0
}
